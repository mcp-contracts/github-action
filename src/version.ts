/** Version of this action, kept in sync with package.json and the monorepo release. */
export const ACTION_VERSION = "0.7.3";

/** Tool identifier written into captured snapshots. */
export const ACTION_TOOL = `mcp-contracts-action/${ACTION_VERSION}`;
